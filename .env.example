# Backend de procesamiento
#   local  = 100% offline, sin dependencias externas (default)
#   cloud  = requiere GEMINI_API_KEY y google-genai instalado
ATC_BACKEND=local

# Solo necesario si ATC_BACKEND=cloud
# GEMINI_API_KEY=your-gemini-api-key-here
